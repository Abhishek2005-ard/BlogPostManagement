services:
  # ── BlogCraft App ──────────────────────────────────────────────────────────
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: blogcraft-app
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      NODE_ENV:       production
      PORT:           5000
      MONGO_URI:      mongodb+srv://dhatraka77:Abhishek2005@cluster0.kaxbrvj.mongodb.net/mern_app?retryWrites=true&w=majority&appName=Cluster0
      JWT_SECRET:     ${JWT_SECRET:-change_me_in_production}
      JWT_EXPIRES_IN: 7d
      CLIENT_URL:     http://localhost:5000
